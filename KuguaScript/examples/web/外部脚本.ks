// 外部 .ks 文件 — 由 kugua-loader.js 自动编译执行

说（“外部脚本开始运行。”）。
段落：文档之创建元素（“p”）。
段落之设置文本（“这一段是外部 .ks 文件创建出来的。”）。
段落之设置样式（“color”，“green”）。
段落之设置样式（“fontWeight”，“bold”）。
文档之身体之追加子节点（段落）。

当前时间：日期之now（）。
说（“当前时间：” + 当前时间）。
