// ============================================
// 苦瓜脚本教程 02：运算符与"的"语缀
// ============================================

// 一、算术：+ - * / %
说（1 + 2）。
说（10 - 3）。
说（4 * 5）。
说（20 / 4）。
说（10 % 3）。

// 二、比较
说（1 小于 2）。
说（2 大于 1）。
说（1 等于 1）。
说（1 是 1）。           // 是 与 等于 等价
说（1 不是 2）。
说（1 小于等于 1）。
说（2 不小于 1）。
说（2 不大于 1）。

// 三、逻辑：并且 / 或者 / 非
说（正确 并且 正确）。
说（正确 或者 错误）。
说（非 正确）。
说（1 是 1 并且 2 是 2）。

// 四、"的"是无实义语缀
说（正确的）。
说（真的）。
说（错的）。
说（不对的）。
说（空的）。
说（真的是 正确）。

// 五、包含：X 包含 Y → X.includes(Y)
说（“你好世界”包含 “世界”）。
