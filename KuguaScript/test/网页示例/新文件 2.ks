// 苦瓜脚本示例（新语法）

// 语料库：数组
通用回答：“你说你马呢？”、“不会真有人觉得是这样吧？不会吧不会吧？”、“就这？就这？”、“你品，你细品”。
强烈语气符号：“！”、“？？？”。
强烈语气回答：“他急了！他急了！”、“你急了？”。
问句标志：“？”、“怎么”、“什么”、“咋”。
问句回复：“你都不知道，那你说你马呢？”、“不会真有人还不知道吧？”。

强制性全局问题长度：0。

// 判断 问句 是否包含 标志组 中的某一项
检查类型：输入 问句、标志组；
	重复（开始于i=1，到i小于等于 标志组之长度 为止，每次i++）：
		如果（问句包含 标志组第i项）：
			结果是 正确。
	结果是 错误。

// 从数组中随机选一项
随机选择：输入 数组；
	结果是 数组之第（向下取整（随机数字（） * 数组之长度） + 1）项。

// 根据问题语气挑选回复
拣选结果：输入 问题；
	如果（随机数字（） 小于 强制性全局问题长度）：
		结果是 随机选择（通用回答）。
	如果（检查类型（问题，强烈语气符号））：
		结果是 随机选择（强烈语气回答）。
	否则，如果（检查类型（问题，问句标志））：
		结果是 随机选择（问句回复）。
	否则：
		结果是 随机选择（通用回答）。


// 沙箱交互部分（在编辑器沙箱视图中运行）
文本区域：空。
文本框：空。

生成：
	问题：文本框之值。
	如果 问题 等价于 “”：结束。
	回答：拣选结果（问题）。
	文本区域之值：文本区域之值 + 问题 + “\n” + 回答 + “\n\n”。
	文本框之值：空。
	文本区域之滚动条顶部：文本区域之滚动条长度。

窗体加载处理：输入；
	文本区域：文档之获取元素按id（“文本区域”）。
	文本框：文档之获取元素按id（“输入”）。
	文本框之添加事件监听（“keydown”，按键处理）。

按键处理：输入 事件；
	如果 事件之键 等价于 “Enter”：
		事件之处置上级默认。
		生成。

窗体加载处理（）。
