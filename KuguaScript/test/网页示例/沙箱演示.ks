// ========================================
// 沙箱能力演示 — DOM操作 + 事件监听 + 本地存储
// 此脚本在 iframe 沙箱中运行，能直接操作页面
// ========================================

说（“沙箱演示开始”）。
说（“”）。

// ========== 一、DOM 操作 ==========
说（“【一、DOM操作】”）。

// 创建标题
标题：文档之创建元素（“h1”）。
标题之设置文本（“苦瓜脚本沙箱演示”）。
标题之设置样式（“color”，“#e74c3c”）。
标题之设置样式（“textAlign”，“center”）。
文档之身体之追加子节点（标题）。

// 创建段落
段落：文档之创建元素（“p”）。
段落之设置文本（“这段文字由苦瓜脚本生成！”）。
段落之设置样式（“fontSize”，“18px”）。
文档之身体之追加子节点（段落）。

// 创建按钮
按钮：文档之创建元素（“button”）。
按钮之设置文本（“点击我”）。
按钮之设置样式（“padding”，“10px 20px”）。
按钮之设置样式（“fontSize”，“16px”）。
按钮之设置样式（“background”，“#3498db”）。
按钮之设置样式（“color”，“#fff”）。
按钮之设置样式（“border”，“none”）。
按钮之设置样式（“borderRadius”，“5px”）。
按钮之设置样式（“cursor”，“pointer”）。
文档之身体之追加子节点（按钮）。

说（“已创建标题、段落、按钮”）。
说（“”）。

// ========== 二、事件监听 ==========
说（“【二、事件监听】”）。
说（“尝试点击沙箱中的按钮！”）。

点击次数：0。

// 定义点击处理函数
点击处理：输入“事件”；
    点击次数：点击次数 + 1。
    说（“按钮被点击了！第” + 点击次数 + “次”）。
    段落之设置文本（“按钮已被点击” + 点击次数 + “次。苦瓜脚本正在运行！”）。
    如果（点击次数 是 5）：
        段落之设置样式（“color”，“#e74c3c”）。
        段落之设置文本（“点击已达5次，文字变红！”）。

按键处理：输入“事件”；
    说（“检测到键盘按键事件”）。

按钮之添加事件监听（“click”，点击处理）。
窗口之添加事件监听（“keydown”，按键处理）。

说（“事件监听已绑定，请在沙箱视图中点击按钮测试。”）。
说（“”）。

// ========== 三、本地存储 ==========
说（“【三、本地存储】”）。

访问次数：本地存储之读取（“访问次数”）。
如果（访问次数 是 空）：
    访问次数：1。
否则：
    访问次数：转整数（访问次数） + 1。

本地存储之保存（“访问次数”，访问次数）。
说（“这是你第” + 访问次数 + “次访问沙箱（数据存于localStorage）”）。
说（“”）。

// ========== 四、键盘事件 ==========
说（“【四、键盘事件】”）。
说（“在沙箱视图内按键试试（聚焦iframe后）”）。
说（“键盘事件已绑定到按键处理函数。”）。

说（“”）。
说（“沙箱演示完成。所有交互在下方沙箱视图中进行。”）。
