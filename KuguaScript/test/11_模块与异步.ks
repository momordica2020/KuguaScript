// ============================================
// 苦瓜脚本教程 11：模块与异步
// 引入/导出 为 Node 端（浏览器打包工具亦可）
// ============================================

// 一、引入：引入 “模块名” 作为 名字（或 称作）
引入 “path” 作为 路径模块。
引入 “os” 称作 系统模块。
说（路径模块之join（“a”，“b”））。
说（系统模块之platform（））。

// 二、导出（编译为 CommonJS module.exports）
答案值：42。
导出 答案值 作为 答案。

// 三、等待（await）：顶层异步运行
引入 “timers/promises” 作为 定时器。
说（“开始”）。
等待 定时器之setTimeout（10）。
说（“结束”）。

// 四、异步函数（函数内出现 等待 自动 async）
延迟一下：输入 毫秒；
    等待 定时器之setTimeout（毫秒）。
    结果是 “完成”。
说（等待 延迟一下（5））。

// 五、浏览器异步示例（仅展示语法，Node 不可运行）
// 数据：等待 获取（“https://api.example.com/data”）。
