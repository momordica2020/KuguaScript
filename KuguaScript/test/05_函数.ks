// ============================================
// 苦瓜脚本教程 05：函数
// ============================================

// 一、定义与调用：名字：输入 参数；函数体（缩进）
问候：输入 名字；
    结果是 “你好，” + 名字。
说（问候（“小明”））。

// 二、多参数（顿号分隔）
加法：输入 a、b；
    结果是 a + b。
说（加法（3，4））。

// 三、无参数函数
当前时间：输入；
    结果是 日期之now（）。
说（当前时间（） 大于 0）。

// 四、默认参数：缺省 为
欢迎：输入 名字 缺省 为 “路人”；
    结果是 “你好，” + 名字。
说（欢迎（“小明”））。
说（欢迎（））。

// 五、"如下"函数头（无参）
打个招呼 如下：
    说（“你好呀”）。
打个招呼（）。

// 六、递归
阶乘：输入 n；
    如果 n 小于等于 1：结果是 1。
    结果是 n * 阶乘（n-1）。
说（阶乘（5））。

// 七、函数引用可作参数（回调）
翻倍：输入 x；
    结果是 x*2。
映射测试：输入 数组、处理；
    结果是 列表之映射（数组，处理）。
说（映射测试（【1、2、3】，翻倍））。
