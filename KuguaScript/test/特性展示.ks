// 苦瓜脚本语言特性展示

// 一、变量定义
数：42。
字符串：“你好，苦瓜！”。
小数：3.14。
布尔值：正确。
空值：空。

说（数）。
说（字符串）。
说（小数）。
说（布尔值）。
说（空值）。

// 二、算术运算
加法结果：1 + 2。
减法结果：10 - 3。
乘法结果：4 * 5。
除法结果：20 / 4。
取余结果：10 % 3。

说（加法结果）。
说（减法结果）。
说（乘法结果）。
说（除法结果）。
说（取余结果）。

// 三、字符串拼接
问候：“你好，” + “世界！”。
说（问候）。

// 四、条件语句
如果（数 小于 50）：
    说（“数小于50”）。
否则：
    说（“数大于等于50”）。

// 五、否则如果（多条件分支）
如果（数 小于 10）：
    说（“一位数”）。
否则如果（数 小于 100）：
    说（“两位数”）。
否则：
    说（“三位数或更大”）。

// 六、循环语句（重复）
说（“循环开始”）。
重复（开始于i=0，到i小于3为止，每次i++）：
    说（i）。
说（“循环结束”）。

// 七、比较运算
说（1 小于 2）。
说（2 大于 1）。
说（1 等于 1）。
说（1 小于等于 1）。
说（1 大于等于 1）。
说（1 不大于 2）。
说（2 不小于 1）。

// 八、逻辑运算
说（正确 并且 正确）。
说（正确 或者 错误）。
说（非 正确）。

// 九、函数定义和调用
问候函数：输入“名字”；
    结果是“你好，” + 名字。

说（问候函数（“苦瓜”））。

// 十、类（对象）定义和方法
计算器：
    加法：输入“a”、“b”；
        结果是a + b。
    减法：输入“a”、“b”；
        结果是a - b。
    乘法：输入“a”、“b”；
        结果是a * b。

说（计算器之加法（3，5））。
说（计算器之减法（10，4））。
说（计算器之乘法（6，7））。

// 十一、嵌套调用
说（计算器之加法（计算器之加法（1，2），3））。
