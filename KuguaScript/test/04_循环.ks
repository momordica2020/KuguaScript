// ============================================
// 苦瓜脚本教程 04：循环
// ============================================

// 一、重复（for）：开始于 变量=初值，到 条件 为止，每次 更新
重复（开始于i=0，到i小于5为止，每次i++）：
    说（i）。

// 二、循环 N 次（循环变量）
循环 3 次（序号）：
    说（“第” + 序号 + “次”）。

// 三、当…时（while）
n：0。
当 n 小于 3 时：
    说（n）。
    n：n+1。

// 四、重复 直到…为止（do-while：至少执行一次，条件满足后停止）
m：5。
重复 直到 m 大于等于 8 为止：
    m：m+1。
说（m）。

// 五、每当…就（口语 while）
k：0。
每当 k 小于 3，就 把 k 增加 1。
说（k）。

// 六、遍历 列表 中 的 每个 元素（for-of）
名单：【“甲”、“乙”、“丙”】。
遍历 名单 中 的 每个 元素：
    说（元素）。

// 七、结束（break）
重复（开始于i=0，到i小于100为止，每次i++）：
    如果 i 是 3：结束。
    说（i）。
