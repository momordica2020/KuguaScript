样式：文档之创建元素（“style”）。
样式之设置文本（“table{border-collapse:collapse}td{width:18px;height:18px;text-align:center;font-size:14px;border:1px solid #e5e7eb}button{margin:2px;padding:6px 14px;border:0;border-radius:6px;cursor:pointer;font-size:14px}.hd{display:none!important}.up{background:#10b981;color:#fff}.ov{background:#8b5cf6;color:#fff}.re{background:#ef4444;color:#fff}.st{background:#0ea5e9;color:#fff}”）。
tmp：文档之头部。
tmp之追加子节点（样式）。

宽：20。
高：20。
蛇身：《210、209、208》。
dir：“右”。
nextD：“右”。
食：215。
分：0。
速：220。
运行：错误。
done：错误。
计号：空。

页：文档之创建元素（“div”）。
页之设置样式（“padding”，“16px”）。
页之设置样式（“fontFamily”，“sans-serif”）。
页之设置样式（“backgroundColor”，“#ecfdf5”）。
tmp：文档之身体。
tmp之设置样式（“margin”，“0”）。
tmp之追加子节点（页）。

标题：文档之创建元素（“h1”）。
标题之设置文本（“🐍 贪吃蛇”）。
标题之设置样式（“margin”，“0 0 8px 0”）。
标题之设置样式（“color”，“#065f46”）。
页之追加子节点（标题）。

息：文档之创建元素（“div”）。
息之设置文本（“分数：0   长度：3”）。
息之设置样式（“fontSize”，“16px”）。
息之设置样式（“marginBottom”，“10px”）。
息之设置样式（“fontWeight”，“bold”）。
息之设置样式（“color”，“#047857”）。
页之追加子节点（息）。

表：文档之创建元素（“table”）。
页之追加子节点（表）。
单：空。
循环 高 次（_i）：
  tr：文档之创建元素（“tr”）。
  表之追加子节点（tr）。
  循环 宽 次（_j）：
    td：文档之创建元素（“td”）。
    td之设置文本（“·”）。
    td之设置样式（“color”，“#d1d5db”）。
    tr之追加子节点（td）。
    td之设置属性（“id”，“c”+（_i*宽+_j））。

设格：输入“id”、“字”、“色”；
  tmpx：文档之获取元素按id（id）。
  tmpx之设置文本（字）。
  tmpx之设置样式（“color”，色）。

清：输入“无”；
  k：0。
  循环 宽*高 次（_i）：
    设格（“c”+k，“·”，“#d1d5db”）。
    k：k+1。

渲染：输入“无”；
  清（）。
  设格（“c”+食，“★”，“#f59e0b”）。
  循环 蛇身之长度 次（_i）：
    设格（“c”+蛇身之第（_i+1）项，“●”，“#111827”）。

撞身：输入“n”；
  循环 蛇身之长度 次（_i）：
    如果（n 是 蛇身之第（_i+1）项）：结果是 正确。
  结果是 错误。

死：输入“无”；
  done：正确。
  运行：错误。
  如果（计号 不是 空）：stopLoop（计号）。
  计号：空。
  息之设置文本（“💀 结束！最终分数：”+分）。
  显（“R”）。
  隐（“P”）。
  隐（“S”）。

走：输入“无”；
  dir：nextD。
  新头：蛇身之第1项。
  xx：新头-向下取整（新头/宽）*宽。
  yy：向下取整（新头/宽）。
  选择（dir）：
    情况 “上”：
      如果（yy 是 0）：死（）。返回。
      新头：新头-宽。
    情况 “下”：
      如果（yy 是 高-1）：死（）。返回。
      新头：新头+宽。
    情况 “左”：
      如果（xx 是 0）：死（）。返回。
      新头：新头-1。
    情况 “右”：
      如果（xx 是 宽-1）：死（）。返回。
      新头：新头+1。
  吃：新头 是 食。
  如果（吃）：
    蛇身之插入头部（新头）。
    分：分+1。
    如果（速 大于 80）：速：速-6。
    重生（）。
  否则：
    蛇身之移除尾部（）。
    如果（撞身（新头））：死（）。返回。
    蛇身之插入头部（新头）。
  如果（运行 并且 非 done）：
    渲染（）。
    息之设置文本（“分数：”+分+“   长度：”+蛇身之长度）。
    如果（计号 不是 空）：stopLoop（计号）。
    计号：startLoop（走，速）。

重生：输入“无”；
  n：向下取整（随机数字（）*宽*高）。
  循环 500 次（_i）：
    如果（非 撞身（n））：
      食：n。
      返回。
    n：n+1。
    如果（n 大于等于 宽*高）：n：0。

方向：输入“n”；
  如果（dir 是 “上” 并且 n 是 “下”）：返回。
  如果（dir 是 “下” 并且 n 是 “上”）：返回。
  如果（dir 是 “左” 并且 n 是 “右”）：返回。
  如果（dir 是 “右” 并且 n 是 “左”）：返回。
  nextD：n。
  如果（非 运行）：
    如果（done）：重开（）。
    开动（）。
  否则：
    如果（非 done）：走（）。

处理按键：输入“ev”；
  k：ev之键。
  如果（k 是 “ArrowUp”）：k：“w”。
  如果（k 是 “ArrowDown”）：k：“s”。
  如果（k 是 “ArrowLeft”）：k：“a”。
  如果（k 是 “ArrowRight”）：k：“d”。
  选择（k）：
    情况 “w”：
      方向（“上”）。
    情况 “s”：
      方向（“下”）。
    情况 “a”：
      方向（“左”）。
    情况 “d”：
      方向（“右”）。

上移：输入“e”；
  方向（“上”）。

下移：输入“e”；
  方向（“下”）。

左移：输入“e”；
  方向（“左”）。

右移：输入“e”；
  方向（“右”）。

开动：输入“无”；
  运行：正确。
  隐（“S”）。
  显（“P”）。
  显（“R”）。
  计号：startLoop（走，速）。

开始：输入“e”；
  如果（done）：重开（）。
  开动（）。
  走（）。

暂停：输入“e”；
  运行：错误。
  如果（计号 不是 空）：stopLoop（计号）。
  计号：空。
  显（“S”）。
  隐（“P”）。

重开：输入“e”；
  如果（计号 不是 空）：stopLoop（计号）。
  计号：空。
  蛇身：《210、209、208》。
  dir：“右”。
  nextD：“右”。
  食：215。
  分：0。
  速：220。
  运行：错误。
  done：错误。
  息之设置文本（“分数：0   长度：3”）。
  渲染（）。
  显（“S”）。
  隐（“P”）。
  隐（“R”）。

造b：输入“文”、“cls”、“处理”、“iid”；
  b：文档之创建元素（“button”）。
  b之设置文本（文）。
  b之设置属性（“id”，“B”+iid）。
  b之设置样式（“padding”，“6px 14px”）。
  b之设置样式（“margin”，“2px”）。
  b之设置样式（“border”，“0”）。
  b之设置样式（“borderRadius”，“6px”）。
  b之设置样式（“cursor”，“pointer”）。
  b之设置样式（“fontSize”，“14px”）。
  选择（cls）：
    情况 “up”：
      b之设置样式（“background”，“#10b981”）。
      b之设置样式（“color”，“#fff”）。
    情况 “ov”：
      b之设置样式（“background”，“#8b5cf6”）。
      b之设置样式（“color”，“#fff”）。
    情况 “re”：
      b之设置样式（“background”，“#ef4444”）。
      b之设置样式（“color”，“#fff”）。
    情况 “st”：
      b之设置样式（“background”，“#0ea5e9”）。
      b之设置样式（“color”，“#fff”）。
    情况 “hd”：
      b之设置样式（“display”，“none”）。
  b之添加事件监听（“click”，处理）。
  页之追加子节点（b）。

显：输入“i”；
  tmp8：文档之获取元素按id（“B”+i）。
  tmp8之设置样式（“display”，“inline-block”）。

隐：输入“i”；
  tmp9：文档之获取元素按id（“B”+i）。
  tmp9之设置样式（“display”，“none”）。

渲染（）。
造b（“↑ 上”，“up”，上移，“U”）。
造b（“↓ 下”，“up”，下移，“D”）。
造b（“← 左”，“up”，左移，“L”）。
造b（“→ 右”，“up”，右移，“R0”）。
tmpbr1：文档之创建元素（“br”）。
页之追加子节点（tmpbr1）。
造b（“▶ 开始”，“st”，开始，“S”）。
造b（“⏸ 暂停”，“ov”，暂停，“P”）。
造b（“🔄 重开”，“re”，重开，“R”）。
隐（“P”）。
隐（“R”）。
窗口之添加事件监听（“keydown”，处理按键）。