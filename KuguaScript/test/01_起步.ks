// ============================================
// 苦瓜脚本教程 01：起步
// 注释、字面量、输出、变量、把/设字句
// 运行：node cli.js test/01_起步.ks
// ============================================

// 一、注释
// 单行注释用 //，多行用 /* … */

// 二、输出：说（…）
说（“你好，苦瓜脚本！”）。

// 三、字面量
说（42）。              // 整数
说（3.14）。            // 小数
说（“字符串”）。        // 字符串用中文引号
说（正确）。            // 布尔：正确 / 错误（也可 真/对、假/错）
说（空）。              // 空值

// 四、变量定义：名字 ： 值
数：42。
字符串：“你好”。
布尔：正确。
说（数）。
说（字符串 + “，世界！”）。   // 字符串拼接用 +

// 五、"是"也可作赋值号（成员赋值尤其好用）
数 是 100。
说（数）。

// 六、把/设字句（赋值语法糖）
把 数 增加 10。         // 数 += 10
说（数）。
把 数 减少 5。          // 数 -= 5
说（数）。
设 数 为 7。            // 数 = 7
说（数）。
将 数 设为 99。         // 数 = 99
说（数）。

// 七、同一行多条语句用逗号分隔
a：1，b：2。
说（a + b）。
