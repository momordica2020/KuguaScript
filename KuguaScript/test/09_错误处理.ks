// ============================================
// 苦瓜脚本教程 09：错误处理
// ============================================

// 制造错误的辅助函数：访问空值属性会抛错
触发错误：输入 无；
    空之不存在属性。

// 一、尝试…如果报错
尝试：
    说（“开始”）。
    触发错误（）。
如果报错：
    说（“已捕获”）。

// 二、绑定错误变量
尝试：
    触发错误（）。
如果报错（错误信息）：
    说（“出错了：” + 错误信息）。

// 三、假如报错 / 要是报错
尝试：
    说（“正常执行”）。
假如报错：
    说（“不会执行”）。

尝试：
    触发错误（）。
要是报错：
    说（“要是分支也捕获”）。

// 四、行内写法
尝试：触发错误（），如果报错：说（“行内捕获”）。

// 五、未出错时跳过捕获分支
尝试：
    说（“无错执行”）。
如果报错：
    说（“不会执行”）。
