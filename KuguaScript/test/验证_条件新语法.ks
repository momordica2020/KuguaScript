// 验证：条件语句新语法 —— 条件可不加括号，分支可用冒号或逗号结尾
成绩：85。

// 一、无括号条件 + 冒号结尾
如果 成绩 大于等于 90：
    说（“无括号冒号：优秀”）。
否则如果 成绩 大于等于 80：
    说（“无括号冒号：良好”）。
否则：
    说（“无括号冒号：不及格”）。

// 二、无括号条件 + 逗号结尾
如果 成绩 大于等于 80，
    说（“逗号分支：良好”）。
否则，
    说（“逗号分支：不及格”）。

// 三、传统写法（括号）+ 逗号结尾
如果（成绩 大于等于 60），
    说（“括号逗号：及格”）。

// 四、否则如果链（逗号/冒号混用）
如果 成绩 大于等于 90：
    说（“链：优”）。
否则，如果 成绩 大于等于 85，
    说（“链：良”）。
否则如果 成绩 大于等于 60，
    说（“链：及格”）。
否则：
    说（“链：差”）。

// 五、同一行内的逗号仍然是逻辑或
如果 错误，正确：
    说（“同行逗号是或：成立”）。

// 六、嵌套条件
如果 正确，
    如果 成绩 大于等于 85，
        说（“嵌套：内层成立”）。
    否则，
        说（“嵌套：内层不成立”）。
