// ============================================
// 苦瓜脚本教程 12：综合实战——文本词频统计
// 综合运用：数组、文本库、遍历、把字句、函数、流水句、排序
// ============================================

原文：“苦瓜 苦瓜 苹果 苦瓜 苹果 香蕉”。
词表：文本之拆分（原文，“ ”）。
结果表：【】。

// 记录一个词：已存在则次数+1，否则新增条目
记录：输入 词；
    遍历 结果表 中 的 每个 条目：
        如果 条目之词 是 词：
            把 条目之次数 增加 1。
            结果是 空。
    新条目：对象。
    新条目之词：词。
    新条目之次数：1。
    列表之追加（结果表，新条目）。

// 统计
遍历 词表 中 的 每个 词：
    记录（词）。

// 按次数从高到低排序
比较条目：输入 a、b；
    结果是 b之次数 - a之次数。
列表之排序（结果表，比较条目）。

// 输出
遍历 结果表 中 的 每个 条目：
    说（条目之词 + “：” + 条目之次数）。

// 汇总
累计次数：输入 合计、条目；
    结果是 合计 + 条目之次数。
说（“总次数：” + 列表之归并（结果表，累计次数，0））。

// 流水句收尾
先 文本之拆分（“春，夏，秋，冬”，“，”），
再 列表之反转（它），
然后 列表之连接（它，“-”），
最后 说（它）。
