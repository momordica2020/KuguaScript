// ============================================
// 苦瓜脚本教程 10：中文标准库
// 数学/日期/文本/列表 浏览器端也可用；
// 路径/网址/加密/系统/环境变量/缓冲/文件/服务器 为 Node 端
// ============================================

// 一、数学
说（数学之平方根（16））。
说（数学之乘方（2，10））。
说（数学之圆周率 大于 3）。

// 二、日期
现在：日期之现在（）。
说（日期之年（现在） 大于 2000）。
说（日期之格式化（现在，“YYYY年MM月DD日”））。

// 三、路径
说（路径之拼接（“a”，“b”））。
说（路径之文件名（“/数据/报告.txt”））。
说（路径之扩展名（“/数据/报告.txt”））。

// 四、网址
网址对象：网址之解析（“https://example.com/搜索?关键词=苦瓜”）。
说（网址对象之协议）。
说（网址对象之主机）。
说（网址对象之参数之关键词）。

// 五、加密
说（加密之哈希（“sha256”，“abc”）之长度）。
标识：加密之随机标识（）。
说（标识之长度）。

// 六、系统与环境变量
说（系统之平台（））。
环境变量之设置（“KS_TEST”，“你好”）。
说（环境变量之读取（“KS_TEST”））。
环境变量之删除（“KS_TEST”）。

// 七、缓冲
说（缓冲之转十六进制（缓冲之从文本（“AB”）））。

// 八、文件（使用系统临时目录，不污染项目）
测试文件：路径之拼接（系统之临时目录（）， “ks教程.txt”）。
文件之写入（测试文件，“苦瓜脚本会写文件了”）。
说（文件之存在（测试文件））。
说（文件之读取（测试文件））。
文件之删除（测试文件）。
说（文件之存在（测试文件））。

// 九、HTTP 服务器（创建/关闭，不监听端口）
处理请求：输入 请求、响应；
    响应之状态（200）。
    响应之发送（“你好”）。
服务：服务器之创建（处理请求）。
服务器之关闭（服务）。
说（“服务器创建/关闭正常”）。
