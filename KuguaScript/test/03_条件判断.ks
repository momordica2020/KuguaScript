// ============================================
// 苦瓜脚本教程 03：条件判断
// ============================================

成绩：85。

// 一、如果 / 否则如果 / 否则（括号可省略）
如果（成绩 大于等于 90）：
    说（“优秀”）。
否则如果（成绩 大于等于 60）：
    说（“及格”）。
否则：
    说（“不及格”）。

// 二、省略括号 + 逗号分支
如果 成绩 大于等于 60，
    说（“通过”）。
否则，
    说（“未通过”）。

// 三、行内写法
如果 1 是 1：说（“一行搞定”），否则：说（“不会执行”）。

// 四、选择 / 情况（按值匹配）
等级：2。
选择（等级）：
    情况 1，
        说（“一级”）。
    情况 2：
        说（“二级”）。
    否则：
        说（“其他”）。

// 五、口语后缀"的话"
天气：“晴”。
如果 天气 是 “雨” 的话：
    说（“带伞”）。
否则：
    说（“出门”）。
