// ========================================
// 勇者的试炼（沙箱互动版）
// 在沙箱中渲染DOM卡片UI，用户点击【攻击】/【喝药水】按钮战斗
// ========================================

// ========== 角色数据 ==========
勇者：
    名字：“勇者”。
    生命：100。
    最大生命：100。
    攻击：15。
    金币：0。
    药水：3。

敌人们：
    史莱姆：
        名字：“史莱姆”。
        生命：25。
        最大生命：25。
        攻击：8。
        金币：5。
    哥布林：
        名字：“哥布林”。
        生命：40。
        最大生命：40。
        攻击：12。
        金币：10。
    巨龙：
        名字：“巨龙”。
        生命：70。
        最大生命：70。
        攻击：18。
        金币：30。

关卡索引：0。
游戏结束标志：错误。

// ========== 工具：创建带样式的元素 ==========
造盒子：输入“类名”；
    盒：文档之创建元素（“div”）。
    盒之设置属性（“类”，类名）。
    结果是盒。

造文字：输入“文本”；
    p：文档之创建元素（“div”）。
    p之设置文本（文本）。
    结果是p。

造按钮：输入“文字”、“处理函数”；
    btn：文档之创建元素（“button”）。
    btn之设置文本（文字）。
    btn之设置样式（“padding”，“10px 18px”）。
    btn之设置样式（“margin”，“4px”）。
    btn之设置样式（“border”，“none”）。
    btn之设置样式（“borderRadius”，“6px”）。
    btn之设置样式（“background”，“linear-gradient(135deg,#667eea,#764ba2)”）。
    btn之设置样式（“color”，“#fff”）。
    btn之设置样式（“fontWeight”，“bold”）。
    btn之设置样式（“fontSize”，“14px”）。
    btn之设置样式（“cursor”，“pointer”）。
    btn之设置样式（“transition”，“all .2s”）。
    btn之添加事件监听（“mouseenter”，按钮悬停进入）。
    btn之添加事件监听（“mouseleave”，按钮悬停离开）。
    btn之添加事件监听（“click”，处理函数）。
    结果是btn。

按钮悬停进入：输入“e”；
    e之目标之设置样式（“transform”，“translateY(-1px)”）。
    e之目标之设置样式（“boxShadow”，“0 3px 10px rgba(102,126,234,.4)”）。

按钮悬停离开：输入“e”；
    e之目标之设置样式（“transform”，“”）。
    e之目标之设置样式（“boxShadow”，“”）。

// ========== 页面根容器与样式 ==========
页面：造盒子（“页面”）。
页面之设置样式（“fontFamily”，“Microsoft YaHei, PingFang SC, sans-serif”）。
页面之设置样式（“maxWidth”，“720px”）。
页面之设置样式（“margin”，“0 auto”）。
页面之设置样式（“padding”，“12px”）。
页面之设置样式（“color”，“#1a1a2e”）。
文档之身体之追加子节点（页面）。

大标题：造文字（“勇者的试炼 — 沙箱互动版”）。
大标题之设置样式（“fontSize”，“24px”）。
大标题之设置样式（“fontWeight”，“bold”）。
大标题之设置样式（“textAlign”，“center”）。
大标题之设置样式（“color”，“#e74c3c”）。
大标题之设置样式（“margin”，“8px 0”）。
页面之追加子节点（大标题）。

副标题：造文字（“讨伐巨龙！点击按钮战斗。”）。
副标题之设置样式（“textAlign”，“center”）。
副标题之设置样式（“color”，“#555”）。
副标题之设置样式（“marginBottom”，“10px”）。
页面之追加子节点（副标题）。

// ========== 双卡片容器 ==========
卡片盒：造盒子（“卡片盒”）。
卡片盒之设置样式（“display”，“flex”）。
卡片盒之设置样式（“gap”，“10px”）。
卡片盒之设置样式（“marginBottom”，“10px”）。
页面之追加子节点（卡片盒）。

造角色卡：输入“角色”、“颜色”；
    卡：造盒子（“角色卡”）。
    卡之设置样式（“flex”，“1”）。
    卡之设置样式（“padding”，“12px”）。
    卡之设置样式（“borderRadius”，“10px”）。
    卡之设置样式（“background”，颜色）。
    卡之设置样式（“boxShadow”，“0 2px 10px rgba(0,0,0,.1)”）。

    名字：造文字（角色之名字）。
    名字之设置样式（“fontSize”，“18px”）。
    名字之设置样式（“fontWeight”，“bold”）。
    名字之设置样式（“marginBottom”，“6px”）。
    卡之追加子节点（名字）。

    // 生命条外框
    血条外：造盒子（“血条外”）。
    血条外之设置样式（“height”，“14px”）。
    血条外之设置样式（“background”，“#eee”）。
    血条外之设置样式（“borderRadius”，“7px”）。
    血条外之设置样式（“overflow”，“hidden”）。
    血条外之设置样式（“marginBottom”，“6px”）。
    卡之追加子节点（血条外）。

    血条内：造盒子（“血条内”）。
    血条内之设置样式（“height”，“100%”）。
    血条内之设置样式（“background”，“linear-gradient(90deg,#2ecc71,#27ae60)”）。
    血条内之设置样式（“transition”，“width .3s”）。
    血条外之追加子节点（血条内）。

    // 生命文本
    生命文本：造文字（“生命: ” + 角色之生命 + “ / ” + 角色之最大生命）。
    生命文本之设置样式（“fontSize”，“13px”）。
    生命文本之设置样式（“marginBottom”，“4px”）。
    卡之追加子节点（生命文本）。

    攻金药：造文字（“攻击: ” + 角色之攻击 + “  |  金币: ” + 角色之金币 + “  |  药水: ” + 角色之药水）。
    攻金药之设置样式（“fontSize”，“13px”）。
    卡之追加子节点（攻金药）。

    结果是卡。

// ========== 状态引用（后续更新DOM用） ==========
玩家卡引用：空。
玩家血条内引用：空。
玩家生命文本引用：空。
玩家攻金药引用：空。

敌人卡引用：空。
敌人血条内引用：空。
敌人生命文本引用：空。
当前关卡标题引用：空。

// ========== 关卡标题 ==========
当前关卡标题：造文字（“第一关：遭遇史莱姆！”）。
当前关卡标题之设置样式（“fontSize”，“16px”）。
当前关卡标题之设置样式（“fontWeight”，“bold”）。
当前关卡标题之设置样式（“textAlign”，“center”）。
当前关卡标题之设置样式（“padding”，“6px”）。
当前关卡标题之设置样式（“background”，“#fff3cd”）。
当前关卡标题之设置样式（“borderRadius”，“6px”）。
当前关卡标题之设置样式（“marginBottom”，“10px”）。
当前关卡标题引用：当前关卡标题。
页面之追加子节点（当前关卡标题）。

// ========== 首次渲染卡片 ==========
渲染玩家卡：输入“无”；
    如果（玩家卡引用 不是 空）：
        玩家卡引用之移除子节点（玩家卡引用之第一个子节点）。
    新卡：造角色卡（勇者，“#e8f5e9”）。
    如果（玩家卡引用 不是 空）：
        卡片盒之移除子节点（玩家卡引用）。
    玩家卡引用：新卡。
    卡片盒之追加子节点（新卡）。
    // 保存内部元素引用
    玩家血条内引用：新卡之孩子列表的第2项之第一个子节点。
    玩家生命文本引用：新卡之孩子列表的第3项。
    玩家攻金药引用：新卡之孩子列表的第4项。

渲染敌人卡：输入“无”；
    如果（敌人卡引用 不是 空）：
        卡片盒之移除子节点（敌人卡引用）。
    当前敌：取当前敌人（）。
    新卡：造角色卡（当前敌，“#ffebee”）。
    敌人卡引用：新卡。
    卡片盒之追加子节点（新卡）。
    敌人血条内引用：新卡之孩子列表的第2项之第一个子节点。
    敌人生命文本引用：新卡之孩子列表的第3项。

取当前敌人：输入“无”；
    如果（关卡索引 是 0）：结果是敌人们之史莱姆。
    如果（关卡索引 是 1）：结果是敌人们之哥布林。
    结果是敌人们之巨龙。

// 由于KS暂不支持数组index索引，用硬编码方式刷新
刷新玩家UI：输入“无”；
    百分比：勇者之生命 * 100 / 勇者之最大生命。
    如果（百分比 小于 0）：百分比：0。
    玩家血条内引用之设置样式（“width”，百分比 + “%”）。
    玩家生命文本引用之设置文本（“生命: ” + 勇者之生命 + “ / ” + 勇者之最大生命）。
    玩家攻金药引用之设置文本（“攻击: ” + 勇者之攻击 + “  |  金币: ” + 勇者之金币 + “  |  药水: ” + 勇者之药水）。

刷新敌人UI：输入“无”；
    当前敌：取当前敌人（）。
    百分比：当前敌之生命 * 100 / 当前敌之最大生命。
    如果（百分比 小于 0）：百分比：0。
    敌人血条内引用之设置样式（“width”，百分比 + “%”）。
    敌人生命文本引用之设置文本（“生命: ” + 当前敌之生命 + “ / ” + 当前敌之最大生命）。

// ========== 日志区 ==========
日志外框：造盒子（“日志外框”）。
日志外框之设置样式（“background”，“#263238”）。
日志外框之设置样式（“color”，“#a6e22e”）。
日志外框之设置样式（“fontFamily”，“Consolas, Monaco, monospace”）。
日志外框之设置样式（“fontSize”，“13px”）。
日志外框之设置样式（“padding”，“8px”）。
日志外框之设置样式（“borderRadius”，“8px”）。
日志外框之设置样式（“maxHeight”，“160px”）。
日志外框之设置样式（“overflowY”，“auto”）。
日志外框之设置样式（“marginBottom”，“10px”）。
页面之追加子节点（日志外框）。

加日志：输入“文字”；
    行：造文字（“• ” + 文字）。
    行之设置样式（“lineHeight”，“1.6”）。
    日志外框之追加子节点（行）。
    日志外框之设置属性（“scrollTop”，日志外框之滚动高度）。

// ========== 按钮区 ==========
按钮区：造盒子（“按钮区”）。
按钮区之设置样式（“textAlign”，“center”）。
按钮区之设置样式（“marginBottom”，“10px”）。
页面之追加子节点（按钮区）。

攻击按钮引用：空。
药水按钮引用：空。
下一关按钮引用：空。
重开按钮引用：空。

隐藏元素：输入“el”；
    el之设置样式（“display”，“none”）。

显示元素：输入“el”；
    el之设置样式（“display”，“inline-block”）。

// ========== 战斗逻辑 ==========
玩家攻击：输入“无”；
    如果（游戏结束标志）：结果是。
    当前敌：取当前敌人（）。
    勇者之攻击之值：勇者之攻击。
    伤害：勇者之攻击之值。
    当前敌之生命：当前敌之生命 - 伤害。
    加日志（“勇者攻击” + 当前敌之名字 + “，造成” + 伤害 + “点伤害。”）。
    刷新敌人UI（）。
    如果（当前敌之生命 小于等于 0）：
        加日志（当前敌之名字 + “ 被击败！获得 ” + 当前敌之金币 + “ 金币。”）。
        勇者之金币：勇者之金币 + 当前敌之金币。
        刷新玩家UI（）。
        进入胜利状态（）。
        结果是。
    // 敌人反击
    设置定时器（敌人反击，300）。

敌人反击：输入“无”；
    如果（游戏结束标志）：结果是。
    当前敌：取当前敌人（）。
    勇者之生命：勇者之生命 - 当前敌之攻击。
    加日志（当前敌之名字 + “反击，造成 ” + 当前敌之攻击 + “ 点伤害。”）。
    刷新玩家UI（）。
    如果（勇者之生命 小于等于 0）：
        勇者之生命：0。
        刷新玩家UI（）。
        加日志（“勇者被击败了... 游戏结束。”）。
        游戏结束标志：正确。
        显示元素（重开按钮引用）。
        隐藏元素（攻击按钮引用）。
        隐藏元素（药水按钮引用）。
        当前关卡标题引用之设置文本（“💀 游戏结束 — 勇者倒下了”）。
        当前关卡标题引用之设置样式（“background”，“#f8d7da”）。
        当前关卡标题引用之设置样式（“color”，“#721c24”）。

玩家喝药：输入“无”；
    如果（游戏结束标志）：结果是。
    如果（勇者之药水 小于等于 0）：
        加日志（“没有药水了！”）。
        结果是。
    勇者之药水：勇者之药水 - 1。
    回复：25。
    勇者之生命：勇者之生命 + 回复。
    如果（勇者之生命 大于 勇者之最大生命）：
        勇者之生命：勇者之最大生命。
    加日志（“使用药水，回复 ” + 回复 + “ 点生命。”）。
    刷新玩家UI（）。
    // 敌人反击
    设置定时器（敌人反击，300）。

进入胜利状态：输入“无”；
    隐藏元素（攻击按钮引用）。
    隐藏元素（药水按钮引用）。
    如果（关卡索引 是 2）：
        // 已通关
        游戏结束标志：正确。
        显示元素（重开按钮引用）。
        当前关卡标题引用之设置文本（“🏆 恭喜通关！你是真正的勇者！”）。
        当前关卡标题引用之设置样式（“background”，“#d4edda”）。
        当前关卡标题引用之设置样式（“color”，“#155724”）。
    否则：
        显示元素（下一关按钮引用）。
        当前关卡标题引用之设置文本（“✔ 本关胜利！点击【进入下一关】继续”）。
        当前关卡标题引用之设置样式（“background”，“#d4edda”）。
        当前关卡标题引用之设置样式（“color”，“#155724”）。

进入下一关：输入“无”；
    关卡索引：关卡索引 + 1。
    // 重置敌人数据（通过新值）
    如果（关卡索引 是 1）：
        敌人们之哥布林之生命：敌人们之哥布林之最大生命。
        当前关卡标题引用之设置文本（“第二关：遭遇哥布林！”）。
    如果（关卡索引 是 2）：
        敌人们之巨龙之生命：敌人们之巨龙之最大生命。
        当前关卡标题引用之设置文本（“最终关：巨龙出现！”）。
    当前关卡标题引用之设置样式（“background”，“#fff3cd”）。
    当前关卡标题引用之设置样式（“color”，“#1a1a2e”）。
    隐藏元素（下一关按钮引用）。
    显示元素（攻击按钮引用）。
    显示元素（药水按钮引用）。
    渲染敌人卡（）。
    刷新敌人UI（）。
    加日志（“—— 新敌人出现 ——”）。

重开游戏：输入“无”；
    // 重置角色
    勇者之生命：勇者之最大生命。
    勇者之金币：0。
    勇者之药水：3。
    敌人们之史莱姆之生命：敌人们之史莱姆之最大生命。
    敌人们之哥布林之生命：敌人们之哥布林之最大生命。
    敌人们之巨龙之生命：敌人们之巨龙之最大生命。
    关卡索引：0。
    游戏结束标志：错误。
    // 清空日志
    空日志循环：重复开始。
    重复（开始于i=0，到i小于999为止，每次i++）：
        如果（日志外框之第一个子节点 是 空）：结束。
        日志外框之移除子节点（日志外框之第一个子节点）。
    // 重建UI
    当前关卡标题引用之设置文本（“第一关：遭遇史莱姆！”）。
    当前关卡标题引用之设置样式（“background”，“#fff3cd”）。
    当前关卡标题引用之设置样式（“color”，“#1a1a2e”）。
    渲染玩家卡（）。
    渲染敌人卡（）。
    刷新玩家UI（）。
    刷新敌人UI（）。
    显示元素（攻击按钮引用）。
    显示元素（药水按钮引用）。
    隐藏元素（下一关按钮引用）。
    隐藏元素（重开按钮引用）。
    加日志（“游戏重开。祝你好运！”）。

// ========== 构建按钮 ==========
构建攻击按钮：输入“无”；
    b：造按钮（“⚔ 攻击”，玩家攻击）。
    按钮区之追加子节点（b）。
    攻击按钮引用：b。

构建药水按钮：输入“无”；
    b：造按钮（“🧪 喝药水（25HP）”，玩家喝药）。
    b之设置样式（“background”，“linear-gradient(135deg,#f093fb,#f5576c)”）。
    按钮区之追加子节点（b）。
    药水按钮引用：b。

构建下一关按钮：输入“无”；
    b：造按钮（“➡ 进入下一关”，进入下一关）。
    b之设置样式（“background”，“linear-gradient(135deg,#11998e,#38ef7d)”）。
    按钮区之追加子节点（b）。
    下一关按钮引用：b。
    隐藏元素（b）。

构建重开按钮：输入“无”；
    b：造按钮（“🔄 重新开始”，重开游戏）。
    b之设置样式（“background”，“linear-gradient(135deg,#fa709a,#fee140)”）。
    按钮区之追加子节点（b）。
    重开按钮引用：b。
    隐藏元素（b）。

// ========== 初始化 ==========
加日志（“踏上旅程！前方等待你的是怪物与荣耀。”）。
渲染玩家卡（）。
渲染敌人卡（）。
刷新玩家UI（）。
刷新敌人UI（）。
构建攻击按钮（）。
构建药水按钮（）。
构建下一关按钮（）。
构建重开按钮（）。
