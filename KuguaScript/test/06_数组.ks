// ============================================
// 苦瓜脚本教程 06：数组
// ============================================

// 一、数组字面量：【元素、元素、…】
蛇身：【210、209、208】。
说（蛇身）。

// 二、定义右侧可直接顿号列举
名单：1、2、3、4、5。
说（名单）。

// 三、空数组与嵌套
空表：【】。
矩阵：【1、2、3】、【4、5、6】。
说（空表之长度）。
说（矩阵之第2项之第1项）。

// 四、索引：第…项（1 基）
说（名单第1项）。
说（名单第3项）。
i：2。
说（名单第i项）。

// 五、中文数字索引
说（名单第一项）。
说（名单第五项）。

// 六、长度、包含与"在…中"
说（名单之长度）。
说（名单包含 3）。
如果 3 在 名单 中：
    说（“3 在名单中”）。
如果 9 不在 名单 中：
    说（“9 不在名单中”）。

// 七、列表标准库
列表之追加（名单，6）。
列表之反转（名单）。
说（名单第1项）。
说（列表之连接（名单，“-”））。
说（列表之查找（名单，1））。      // 1 基位置，找不到为 0

翻倍：输入 x；
    结果是 x*2。
说（列表之映射（名单，翻倍））。

大数：输入 x；
    结果是 x 大于 3。
说（列表之筛选（名单，大数））。

合计：输入 总和、x；
    结果是 总和 + x。
说（列表之归并（名单，合计，0））。
