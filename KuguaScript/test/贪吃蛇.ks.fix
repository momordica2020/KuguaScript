// 贪吃蛇（沙箱可玩版）

样式：文档之创建元素（“style”）。
样式之设置HTML（“body{margin:0;padding:10px;background:#1e272e;color:#fff;font-family:Microsoft YaHei,sans-serif;text-align:center;}.t{font-size:20px;font-weight:bold;margin:2px 0 6px;color:#ffd32a;text-shadow:0 0 8px #ffd32a;}.b{margin:6px 0;font-size:14px;}.sc{display:inline-block;background:#3742fa;padding:3px 10px;border-radius:6px;margin:0 6px;}.if{display:inline-block;background:#2f3542;padding:3px 10px;border-radius:6px;}.bx{display:inline-block;border:2px solid #57606f;border-radius:6px;padding:2px;background:#000;}.rw{margin:0;padding:0;font-size:0;}.c{display:inline-block;width:18px;height:18px;margin:1px;border-radius:3px;background:#57606f;}.h{background:linear-gradient(135deg,#ff7f50,#ff6348);box-shadow:0 0 6px #ff6348;}.s{background:linear-gradient(135deg,#2ed573,#7bed9f);}.f{background:radial-gradient(circle,#ff4757 40%,#c44569);border-radius:50%;}.btns{margin-top:8px;}.bta{margin:3px;padding:7px 14px;border:0;border-radius:6px;background:linear-gradient(135deg,#3742fa,#5352ed);color:#fff;font-weight:bold;cursor:pointer;font-size:14px;}.bta:hover{transform:translateY(-1px);box-shadow:0 4px 12px rgba(55,66,250,.4);transition:all .2s;}.ow{background:linear-gradient(135deg,#fa709a,#fee140);color:#1a1a2e;}.hd{display:none;}.h2{margin-top:6px;font-size:12px;color:#a4b0be;}.db{margin:4px 0;}”）。
文档之头部之追加子节点（样式）。

宽：20。高：20。

长：3。
s1：210。s2：209。s3：208。
方向：“右”。下一方：“右”。
食：215。
分：0。速：220。
运行：错误。结束：错误。
计号：空。

标：文档之创建元素（“div”）。标之设置属性（“类”，“t”）。标之设置文本（“🐍 贪吃蛇”）。文档之身体之追加子节点（标）。

条：文档之创建元素（“div”）。条之设置属性（“类”，“b”）。文档之身体之追加子节点（条）。
分标：文档之创建元素（“span”）。分标之设置属性（“类”，“sc”）。分标之设置文本（“分数：0”）。条之追加子节点（分标）。
息：文档之创建元素（“span”）。息之设置属性（“类”，“if”）。息之设置文本（“方向按钮控制”）。条之追加子节点（息）。

盒：文档之创建元素（“div”）。盒之设置属性（“类”，“bx”）。文档之身体之追加子节点（盒）。
格：文档之创建元素（“div”）。格之设置属性（“id”，“G”）。盒之追加子节点（格）。

按：文档之创建元素（“div”）。按之设置属性（“类”，“btns”）。文档之身体之追加子节点（按）。
造按：输入“文”、“类”、“处”、“id”；
  b：文档之创建元素（“button”）。b之设置文本（文）。b之设置属性（“类”，“bta”+“ ”+类）。b之设置属性（“id”，“B”+id）。b之添加事件监听（“click”，处）。按之追加子节点（b）。
显：输入“i”；文档之获取元素按id（“B”+i）。之之移除类名（“hd”）。
隐：输入“i”；文档之获取元素按id（“B”+i）。之之添加类名（“hd”）。

提：文档之创建元素（“div”）。提之设置属性（“类”，“h2”）。提之设置文本（“提示：吃食物加分，撞墙或咬自己结束。速：220ms”）。文档之身体之追加子节点（提）。

// ===== 渲染 =====
渲染：输入“无”；
  html：“”。
  重复（开始于y=0，到y小于高为止，每次y++）：
    重复（开始于x=0，到x小于宽为止，每次x++）：
      号：x+y*宽。
      类：“c”。
      如果（号 是 s1）：类：“c h”。
      否则：
        命中：错误。
        重复（开始于i=2，到i小于等于长为止，每次i++）：
          如果（i 是 2）：如果（号 是 s2）：命中：正确。
          如果（i 是 3）：如果（号 是 s3）：命中：正确。
          如果（i 是 4）：如果（号 是 s4）：命中：正确。
          如果（i 是 5）：如果（号 是 s5）：命中：正确。
          如果（i 是 6）：如果（号 是 s6）：命中：正确。
          如果（i 是 7）：如果（号 是 s7）：命中：正确。
          如果（i 是 8）：如果（号 是 s8）：命中：正确。
          如果（i 是 9）：如果（号 是 s9）：命中：正确。
          如果（i 是 10）：如果（号 是 s10）：命中：正确。
          如果（i 是 11）：如果（号 是 s11）：命中：正确。
          如果（i 是 12）：如果（号 是 s12）：命中：正确。
          如果（i 是 13）：如果（号 是 s13）：命中：正确。
          如果（i 是 14）：如果（号 是 s14）：命中：正确。
          如果（i 是 15）：如果（号 是 s15）：命中：正确。
        如果（命中）：类：“c s”。
        否则：如果（号 是 食）：类：“c f”。
      html：html+“<span class=”+“"”+类+“"”+“></span>”。
    html：html+“<br/>”。
  文档之获取元素按id（“G”）。之之设置HTML（html）。
  分标之设置文本（“分数：”+分）。

在身：输入“号”；
  如果（号 是 s1）：结果是正确。
  重复（开始于i=2，到i小于等于长为止，每次i++）：
    如果（i 是 2）：如果（号 是 s2）：结果是正确。
    如果（i 是 3）：如果（号 是 s3）：结果是正确。
    如果（i 是 4）：如果（号 是 s4）：结果是正确。
    如果（i 是 5）：如果（号 是 s5）：结果是正确。
    如果（i 是 6）：如果（号 是 s6）：结果是正确。
    如果（i 是 7）：如果（号 是 s7）：结果是正确。
    如果（i 是 8）：如果（号 是 s8）：结果是正确。
    如果（i 是 9）：如果（号 是 s9）：结果是正确。
    如果（i 是 10）：如果（号 是 s10）：结果是正确。
    如果（i 是 11）：如果（号 是 s11）：结果是正确。
    如果（i 是 12）：如果（号 是 s12）：结果是正确。
    如果（i 是 13）：如果（号 是 s13）：结果是正确。
    如果（i 是 14）：如果（号 是 s14）：结果是正确。
    如果（i 是 15）：如果（号 是 s15）：结果是正确。
  结果是错误。

取尾：输入“无”；
  如果（长 是 1）：结果是 s1。
  如果（长 是 2）：结果是 s2。
  如果（长 是 3）：结果是 s3。
  如果（长 是 4）：结果是 s4。
  如果（长 是 5）：结果是 s5。
  如果（长 是 6）：结果是 s6。
  如果（长 是 7）：结果是 s7。
  如果（长 是 8）：结果是 s8。
  如果（长 是 9）：结果是 s9。
  如果（长 是 10）：结果是 s10。
  如果（长 是 11）：结果是 s11。
  如果（长 是 12）：结果是 s12。
  如果（长 是 13）：结果是 s13。
  如果（长 是 14）：结果是 s14。
  如果（长 是 15）：结果是 s15。
  如果（长 是 16）：结果是 s16。
  如果（长 是 17）：结果是 s17。
  如果（长 是 18）：结果是 s18。
  如果（长 是 19）：结果是 s19。
  如果（长 是 20）：结果是 s20。
  如果（长 是 21）：结果是 s21。
  如果（长 是 22）：结果是 s22。
  如果（长 是 23）：结果是 s23。
  如果（长 是 24）：结果是 s24。
  如果（长 是 25）：结果是 s25。
  如果（长 是 26）：结果是 s26。
  如果（长 是 27）：结果是 s27。
  如果（长 是 28）：结果是 s28。
  如果（长 是 29）：结果是 s29。
  结果是 s30。

头x：输入“无”；结果是 s1 - （向下取整（s1/宽））*宽。
头y：输入“无”；结果是向下取整（s1/宽）。

死：输入“无”；
  结束：正确。运行：错误。
  如果（计号 不是 空）：清除循环（计号）。计号：空。
  息之设置文本（“💀 结束！最终分数：”+分）。
  显（“R”）。隐（“P”）。隐（“S”）。

走：输入“无”；
  方向：下一方。
  新头：s1。
  tx：头x（）。ty：头y（）。
  如果（方向 是 “上”）：如果（ty 是 0）：死（）。结果是。新头：s1-宽。
  如果（方向 是 “下”）：如果（ty 是 高-1）：死（）。结果是。新头：s1+宽。
  如果（方向 是 “左”）：如果（tx 是 0）：死（）。结果是。新头：s1-1。
  如果（方向 是 “右”）：如果（tx 是 宽-1）：死（）。结果是。新头：s1+1。
  吃：（新头 是 食）。
  如果（不是 吃）：
    尾：取尾（）。
    如果（不是（新头 是 尾））：如果（在身（新头））：死（）。结果是。
  否则：
    如果（在身（新头））：死（）。结果是。
  // 移动：先变长如果吃
  如果（吃）：长：长+1。
  // 从长 downto 2 赋值 si = s（i-1）
  如果（长 是 30）：s30：s29。
  如果（长 是 29）：s29：s28。
  如果（长 是 28）：s28：s27。
  如果（长 是 27）：s27：s26。
  如果（长 是 26）：s26：s25。
  如果（长 是 25）：s25：s24。
  如果（长 是 24）：s24：s23。
  如果（长 是 23）：s23：s22。
  如果（长 是 22）：s22：s21。
  如果（长 是 21）：s21：s20。
  如果（长 是 20）：s20：s19。
  如果（长 是 19）：s19：s18。
  如果（长 是 18）：s18：s17。
  如果（长 是 17）：s17：s16。
  如果（长 是 16）：s16：s15。
  如果（长 是 15）：s15：s14。
  如果（长 是 14）：s14：s13。
  如果（长 是 13）：s13：s12。
  如果（长 是 12）：s12：s11。
  如果（长 是 11）：s11：s10。
  如果（长 是 10）：s10：s9。
  如果（长 是 9）：s9：s8。
  如果（长 是 8）：s8：s7。
  如果（长 是 7）：s7：s6。
  如果（长 是 6）：s6：s5。
  如果（长 是 5）：s5：s4。
  如果（长 是 4）：s4：s3。
  如果（长 是 3）：s3：s2。
  如果（长 是 2）：s2：s1。
  s1：新头。
  如果（吃）：
    分：分+1。
    速：速-6。如果（速 小于 80）：速：80。
    重生（）。
  渲染（）。

重生：输入“无”；
  重复（开始于t=0，到t小于500为止，每次t++）：
    候：向下取整（随机数字（）*宽*高）。
    如果（不是 在身（候））：食：候。结束。

// ===== 方向按钮控制 =====
设方向：输入“方”；
  如果（结束）：结果是。
  如果（方向 是 “上” 且 方 是 “下”）：结果是。
  如果（方向 是 “下” 且 方 是 “上”）：结果是。
  如果（方向 是 “左” 且 方 是 “右”）：结果是。
  如果（方向 是 “右” 且 方 是 “左”）：结果是。
  下一方：方。

上：输入“e”；设方向（“上”）。
下：输入“e”；设方向（“下”）。
左：输入“e”；设方向（“左”）。
右：输入“e”；设方向（“右”）。

// ===== 开始/暂停/重开 =====
步：输入“无”；如果（运行 且 不是 结束）：走（）。

开始：输入“e”；
  如果（运行）：结果是。
  如果（结束）：重开（“”）。
  运行：正确。
  息之设置文本（“进行中 速：”+速+“ms”）。
  隐（“S”）。显（“P”）。
  计号：设置循环（步，速）。

暂停：输入“e”；
  如果（不是 运行）：结果是。
  运行：错误。
  如果（计号 不是 空）：清除循环（计号）。计号：空。
  息之设置文本（“⏸ 暂停”）。
  隐（“P”）。显（“S”）。

重开：输入“e”；
  如果（计号 不是 空）：清除循环（计号）。计号：空。
  长：3。s1：210。s2：209。s3：208。
  方向：“右”。下一方：“右”。食：215。
  分：0。速：220。运行：错误。结束：错误。
  息之设置文本（“方向按钮控制”）。
  渲染（）。显（“S”）。隐（“P”）。隐（“R”）。

造按（“▶ 开始”，“”，“开始”，“S”）。
造按（“⏸ 暂停”，“”，“暂停”，“P”）。隐（“P”）。
造按（“🔄 重开”，“ow”，“重开”，“R”）。隐（“R”）。

// 方向按钮
db：文档之创建元素（“div”）。db之设置属性（“类”，“db”）。文档之身体之追加子节点（db）。
sp：文档之创建元素（“span”）。sp之设置样式（“display”，“inline-block”）。sp之设置样式（“width”，“52px”）。db之追加子节点（sp）。
造按2：输入“文”、“处”、“id”；
  b：文档之创建元素（“button”）。b之设置文本（文）。b之设置属性（“类”，“bta”）。b之设置属性（“id”，“D”+id）。b之添加事件监听（“click”，处）。db之追加子节点（b）。
造按2（“▲”，“上”，“U”）。
br：文档之创建元素（“div”）。db之追加子节点（br）。
造按2（“◀”，“左”，“L”）。
造按2（“▼”，“下”，“D”）。
造按2（“▶”，“右”，“R”）。

渲染（）。
